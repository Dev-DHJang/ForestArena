# UI — 행동 버튼과 읽기 전용 HUD

- 기존 왼쪽 4방향 패드와 오른쪽 버튼 수를 보존한다. 첫 오른쪽 버튼의 표시만 `DASH`에서 `ACTION`으로 바꾼다.
- ACTION은 중립 유지 시 가드, 좌우 누름 시 회피로 CombatIntent를 전달한다. 터치 소스는 판정을 계산하지 않는다.
- HUD는 MatchController snapshot의 가드 내구·상태·공중 회피 잔여만 표시한다. `grab_support`·궁극기 버튼은 없다.
