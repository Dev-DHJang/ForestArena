# QA r01

상태: pass.

- 범위: 개발계 PostgreSQL 실행·마이그레이션·백업 도구만 만들었고, Godot 코드, 게임 저장 표, 계정, 온라인·검증계·운영계 구성은 바꾸지 않았다.
- 실행: PostgreSQL `18.6`, 환경 표식 `development`, 마이그레이션 1개, `app` 표 0개를 실제 DB에서 확인했다.
- 정상: `./scripts/db-dev.sh up`, healthcheck, `migrate` 재실행, `down` 뒤 `up`을 통과했다.
- 권한: `forest_arena_app` 계정이 `app` 스키마 표에는 읽기·쓰기를 하고 표 생성은 거부되는 것을 `./scripts/verify-database-runtime.sh`로 확인했다.
- 실패 보호: 확인 인자 없는 `reset`이 거부되고, `backup` 뒤 `restore <파일> --confirm-development`가 개발계 표식을 유지한 채 통과했다.
- 비밀 보호: `database/.env.development`와 개발계 백업 파일이 Git 제외 규칙에 맞는 것을 확인했다.
- 회귀: `./scripts/verify-database.sh`, `./scripts/verify-docs.sh`, `./scripts/verify-harness.sh`, `./scripts/verify.sh`, `git diff --check`를 통과했다.
- 미확인: 실제 Android 기기 검사는 이번 DB 도구 범위와 관계없어 실행하지 않았다. 검증계·운영계 인스턴스와 게임 저장 데이터는 의도적으로 만들지 않았다.
