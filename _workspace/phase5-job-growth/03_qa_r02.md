# QA r02

## 자동 범위

- 스키마 v3/v2/v5와 deprecated enum tombstone, Phase 4 field 9/10 보존.
- 여섯 leaf 최종 profile, 세 비피니시 patch 범위, 여섯 passive/cancel 계약.
- 강공격 PRESS 즉시 발동, cancel recovery 0/end 경계, 잘못된 context·피니시 거부, jump/dash 목표 실제 발동.
- passive trigger/consume filter, 동일 activation 단일 발동, match reset 초기화.
- 개발 런처 장신구·직업 요약, HUD snapshot 비권위, JSONL v2와 frame percentile/pause 제외.

## 결과

- `./scripts/verify.sh`: pass. Phase 0–5, 결정론, UI, 리소스 및 하네스 검사를 포함한다.
- `python3 tools/forest_arena/verify_godot_resources.py --project-root .`: pass, logical asset 345개 및 quality-dependent asset 23개.
- `./scripts/export-debug-android.sh`: pass, `build/android/ForestArena-debug.apk`와 APK 계약 확인.
- `git diff --check`: pass.
- Galaxy S23 Ultra R2 30매치와 10분 측정: 미실행.
- 독립 QA: 미실행.
- 현재 판정: blocked.
