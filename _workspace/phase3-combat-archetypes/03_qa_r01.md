# Phase 3 QA R01

## 자동 증적

- `./scripts/verify.sh`: PASS. Phase 0–3, v2 계약·구버전 거부, 결정론, UI, resource/character contracts, harness를 포함한다.
- `PHASE3_COMBAT_ARCHETYPES`: 기본형·전문 직업 6개 × spacing/aerial/close 봇 3개의 18개 조합을 720 fixed tick씩 두 번 실행해 snapshot hash 일치를 확인한다.
- `python3 tools/forest_arena/verify_godot_resources.py --project-root .`: PASS (345 logical assets, quality variants 23).
- `git diff --check`: PASS.
- `./scripts/export-debug-android.sh`: PASS (`build/android/ForestArena-debug.apk`).

## 미검증 / 차단

- 2026-09-13 사용자 지시에 따라 실기기 연결 테스트는 이번 실행 범위에서 제외했다.
- Galaxy S23 Ultra 실제 기기에서 18개 매치·10분 성능과 독립 QA를 실행하지 못했다.
- `./scripts/verify-android-emulator.sh`는 연결된 emulator가 없어 실행하지 못했다.
- 따라서 Phase 3의 제품 종료 판정은 `blocked`이며 자동 통과를 실기기 통과로 주장하지 않는다.
