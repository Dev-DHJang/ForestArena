# QA r01

## 통과

- `./scripts/verify.sh`: 네 MoveSet의 잡기 슬롯, 가드 통과, 잡기 면역 및 전체 회귀 통과.
- `GRAB` 태그 공격은 가드 중 대상에 `HIT`가 되고, `IMMUNE_GRAB` 규칙 대상에는 `IMMUNE` 결과가 된다.

## 범위 기록

- Android 정식 추가 행동 버튼은 요청에서 제외된 UI 승인 범위여서 추가하지 않았다. 데스크톱 `G` 입력과 전투 권위 동작은 검증했다.
