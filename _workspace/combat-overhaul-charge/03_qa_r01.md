# QA r01

## 통과

- `./scripts/verify.sh`: 차지 입력·원본 데이터 비변경 검사와 전체 회귀 통과.
- 결정론 검사는 같은 입력에서 `ab2ec28062411d46c31e5470c1e879c4347fb877e1a41a59ce827733344d2ccb` hash를 반복 재현했다.

## 확인한 경계

- 누름 때 방향을 고정하고, 해제까지의 이동 입력은 차지 공격의 방향을 바꾸지 않는다.
- 최소 tick 이전 해제는 일반 강공격, 이후 해제는 런타임 복사본의 강화 강공격이다.
- 리소스 스키마 v2는 묵시 추정하지 않고 `resave_combat_v3_resources.gd`로 저장소 데이터가 v3이 됐다.
