# 통합 기록

- 작업 브랜치: `feature/combat-overhaul-a` (`origin/develop`에서 시작).
- 소비자: 전투 컨트롤러, 매치 판정, HUD, 로드아웃 작성기, 계약 테스트.
- 묶음 순서: A 계약·호환성 → B 상태·적중 → C 입력·방어 → D 연계 → E 장신구 → F 효과 → G 직업·표현 분리.
- 각 묶음은 관련 자동 검사와 기본 검사 후 별도 PR로 병합한다.
