# QA 1차 — 시작 기준

- 상태: fix 진행 중.
- 시작 확인: 최신 `origin/develop`의 병합 SHA `3f70dae`에서 브랜치를 만들었다.
- 보호 확인: UI 자산과 forestlight 작업 파일의 미커밋 변경은 전투 변경에서 제외한다.
- 예정 자동 검사: 계약, HP/stock, 적중 우선순위, 입력 우선순위, 연계, 결정론, `verify.sh`, 문서·리소스 검사.
- 실행 증거:
  - `godot --headless --path . -s res://tests/phase1_match_rules.gd`: PASS (HP 피해, 고정 넉백·경직, 45/60 tick, 동시 무승부).
  - `godot --headless --path . -s res://tests/phase2_loadout_contract.gd`: PASS.
  - `godot --headless --path . -s res://tests/phase1_input_ui.gd`: PASS (HP HUD 읽기 전용 포함).
  - `./scripts/verify-docs.sh`: PASS.
- 추가 실행 증거:
  - `godot --headless --path . -s res://tests/combat_overhaul_contract.gd`: PASS (v2 연계, 아래 입력 우선순위, 1회 부활, 반사 원인 ID 차단).
- 남은 fix: 모든 moveset의 v2 직렬화와 `ComboLinkData` 실제 마이그레이션, 효과 데이터·직업 전체 패치, Android export/에뮬레이터 회귀.
- 실제 Android 기기: 아직 미확인. 에뮬레이터 검증과 별개로 통과로 기록하지 않는다.
