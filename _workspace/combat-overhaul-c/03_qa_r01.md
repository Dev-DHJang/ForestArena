# QA 기록 r01 — 묶음 C

- 통과: `./scripts/verify.sh` — 기존 Phase 0/1/2, 결정론, 입력 UI, 전투 계약과 전체 문서·harness 검사를 통과했다.
- 통과: `./scripts/verify-docs.sh`.
- 통과: `python3 tools/forest_arena/verify_godot_resources.py --project-root .` — 논리 자산 345개, 품질 의존 자산 23개를 확인했다.
- 전용 자동 검사: 아래+점프/아래+공격/아래 단독의 우선순위, 가드 해제, snapshot 자원 값, 회피·궁극기 호환 입력, HUD 자원 표기와 CombatRules v3의 구버전 거부를 `combat_overhaul_contract.gd`와 `phase1_input_ui.gd`에서 확인했다.
- 미확인: 실제 Android 기기의 터치·성능·중단/복귀. 이 환경에는 기기가 연결되지 않아 통과로 기록하지 않는다.
