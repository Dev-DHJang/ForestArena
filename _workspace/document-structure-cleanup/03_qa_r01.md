# 문서 구조 품질 확인 1차

## 판정

`pass`

## 확인 내용

- 공용 JSON 두 파일의 SHA-256 값이 이동 전후 동일하다.
- 목적별 7개 폴더와 중앙 색인이 존재한다.
- `docs/` 최상위에는 `INDEX.md`와 `GLOSSARY.md`만 있다.
- 활성 문서, AGENTS, AI 역할 지침, 캐릭터 콘셉트, 스크립트와 테스트에 이전 경로가 남지 않았다.
- 모든 Markdown 상대 링크가 실제 파일을 가리킨다.
- Godot 테스트가 새 JSON 경로에서 공격·외형 데이터를 읽는다.
- 현재 Phase, 전투 규칙, 공용 ID와 ADR 상태는 바뀌지 않았다.

## 실행 결과

- `./scripts/verify-docs.sh`: PASS
- `./scripts/verify-harness.sh`: PASS
- `./scripts/verify.sh`: PASS
- `git diff --check`: PASS

## 독립성 제한

별도 에이전트를 사용하지 않고 같은 작업 주체가 검사했다. 대신 경로 존재, 이전 경로 검색, 링크 해석, JSON 해시와 전체 Godot 검사를 서로 다른 자동 검사로 교차 확인했다.

## 미확인

문서·경로 변경이므로 실제 Android 기기 검증은 필요하지 않아 실행하지 않았다.
