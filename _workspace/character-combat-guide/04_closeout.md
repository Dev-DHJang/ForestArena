# 현재 캐릭터별 전투 가이드 마무리

## 결과

- `docs/gameplay/character-combat-guide.md`를 추가했다.
- `docs/gameplay/combat.md`와 `docs/INDEX.md`에서 새 가이드로 연결했다.
- `scripts/verify-docs.sh`의 필수 문서 목록에 새 가이드를 추가했다.

## 문서에 포함한 내용

- 현재 기본 화면에서 자현만 조작 가능하고 묘령은 정지 훈련 상대, 나비는 미연결이라는 상태
- 터치·키보드 조작과 방향 입력 의미
- 3회 링아웃, 피해율, 무게, 복귀·무적과 밀리는 각도 조절
- 공통 공격 시간, 피해와 밀어내기 수치
- 세 캐릭터의 이동·무게·연속 공격 단계와 중립 특수기 비교
- 자현·묘령·나비별 한 줄 사용법, 강점, 약점, 기본 공격과 추천 입력 흐름
- 공통 위 특수기와 경기장 복귀 판단
- 아직 구현되지 않은 옆·아래 특수기, 막기·회피·잡기·궁극기 구분
- 실제 코드와 데이터 확인 경로

## 검증

- `./scripts/verify-docs.sh`: PASS
- `./scripts/verify.sh`: PASS
- `CHARACTER_COMBAT_GUIDE_CROSSCHECK`: PASS
- `git diff --check`: PASS

## 변경하지 않은 것

- 전투 코드, 캐릭터·공격 수치와 공용 데이터 형식
- 캐릭터 장면과 기본 실행 구성
- 애니메이션과 이미지
- Phase 범위와 ADR 상태

## 버전 관리 상태

- 기존 `feature/combat-resource-ui-baseline` 브랜치의 커밋되지 않은 통합 작업 위에서 작성했다.
- 기존 사용자 변경과 이전 문서 구조 정리가 함께 있어 커밋·push·PR·`develop` 병합은 실행하지 않았다.

## 다음 작업

- 캐릭터 선택 UI가 생기거나 나비가 기본 장면에 연결되면 `가장 먼저 알아둘 현재 상태` 표를 갱신한다.
- 공격 거리, 고유 기술, 재사용 대기시간이나 애니메이션이 구현되면 콘셉트 설명과 현재 구현 차이를 다시 대조한다.
- 전투 수치 변경 뒤 이 가이드와 `scripts/verify-docs.sh`의 필수 문서 검사를 함께 실행한다.
