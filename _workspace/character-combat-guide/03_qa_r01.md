# 캐릭터 전투 가이드 품질 확인 1차

## 판정

`pass`

## 현재 상태 대조

- 기본 장면의 사용자 선택이 자현이고 훈련 상대 선택이 묘령인지 확인했다.
- 나비 전투 장면과 데이터는 존재하지만 기본 장면에 인스턴스가 없는지 확인했다.
- 묘령 훈련 상대에 입력이나 AI가 연결되지 않은 현재 구성을 확인했다.
- 자현·묘령·나비의 이동, 무게, 점프와 대시 수치를 캐릭터 `.tres`와 대조했다.
- 모든 공격 시간, 피해, 밀어내기와 캐릭터별 특수기 값을 세 `MoveSetData`와 대조했다.
- 옆·아래 특수기 무동작, 공중 공격 2회, 위 특수기 공중 1회 제한을 전투 코드와 대조했다.
- 현재 전투 장면에 캐릭터 스프라이트 애니메이션 노드가 없는지 확인했다.

## 표현 확인

- 현재 구현, 데이터만 준비된 상태와 미래 제안을 별도 절로 구분했다.
- tick은 초당 60회 계산과 초 단위 예시를 함께 적었다.
- `survivability`가 현재 전투 계산에 쓰이지 않음을 명시했다.
- 콘셉트상의 공격 거리와 현재 동일한 60×50 공격 영역을 구분했다.
- 각 캐릭터에 한 줄 사용법, 강점, 약점, 공격 단계, 특수기와 추천 흐름이 있다.

## 검사 결과

- `CHARACTER_COMBAT_GUIDE_CROSSCHECK`: PASS
- `./scripts/verify-docs.sh`: PASS
- `./scripts/verify.sh`: PASS
- `git diff --check`: PASS

## 미확인

- 실제 Android 기기 플레이는 실행하지 않았다. 이번 작업은 현재 코드와 데이터의 문서화이며 전투 구현과 터치 UI를 변경하지 않았다.
- 추천 흐름은 코드상 허용되는 입력 순서다. 실제 상대를 상대로 한 밸런스·난이도 평가는 플레이테스트 전이므로 확정하지 않았다.
